# Virtual Environment
.venv/
venv/
ENV/
env/

# Python bytecode & cache
__pycache__/
*.py[cod]
*$py.class
*.pyc

# Large Model Weights (YOLO auto-downloads if missing, avoids GitHub 100MB limit)
*.pt
*.onnx
*.engine

# Dependencies
web/node_modules/
node_modules/

# Cache and temporary data
cache/
*.log

# Environment variables & API keys
.env
.env.local
.env.*.local

# IDE & OS files
.vscode/
.idea/
.DS_Store
Thumbs.db

# Runtime vehicle count database
vehicle_counts.db
count_cameras.json
local/scratch/
*.bak

local/dataset/
local/dataset_helmet/
local/runs/
*_bkk.pt
local/pipeline.log
local/archive/

# Local datasets, training runs and logs (see local/pipeline/)
local/dataset*/
local/runs/
local/logs/
# Test server instance (launch/localhost_8001): own cache, DB, data
local/stage/
weights/

# SQLite databases (live data, backed up by local/pipeline/backup_db.py)
*.db
*.db-journal
*.db-wal
*.db-shm

# React build output: produced by launch/build_web.bat / npm run build, not committed
web/dist/

# AI analyst report caches written next to the data (riskbkk_agent, water_agent)
riskbkk_analysis.json
water_agent.json
